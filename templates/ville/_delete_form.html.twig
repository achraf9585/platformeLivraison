<form method="post" action="{{ path('ville_delete', {'id': ville.id}) }}" onsubmit="return confirm('Vous êtes sure de supprimer cette ville ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token"  value="{{ csrf_token('delete' ~ ville.id) }}">
    <button class="btn btn-danger" style="width: 50%;margin-left: 380px;margin-top: -60px">Supprimer</button>
</form>
