<form method="post" action="{{ path('region_delete', {'id': region.id}) }}" onsubmit="return confirm('Vous êtes sure de supprimer cette region ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token"  value="{{ csrf_token('delete' ~ region.id) }}">
    <button class="btn btn-danger" style="width: 50%;margin-left: 380px;margin-top: -103px">Supprimer</button>
</form>
